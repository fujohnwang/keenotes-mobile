name: Android Build

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. 安装 GraalVM 21
      - name: Setup GraalVM
        uses: actions/setup-java@v4
        with:
          distribution: 'graalvm' 
          java-version: '21'

      # 2. 🔴 关键修复步骤：显式设置 GRAALVM_HOME
      # GluonFX 插件强制检查 GRAALVM_HOME 变量，我们手动将其指向 JAVA_HOME
      - name: Set GRAALVM_HOME
        run: echo "GRAALVM_HOME=$JAVA_HOME" >> $GITHUB_ENV

      # 3. 缓存依赖
      - name: Cache Maven and Gluon dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gluon
          key: ${{ runner.os }}-maven-gluon-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-gluon-

      # 4. 执行构建
      # 注意：因为我们手动指定了 GraalVM，Gluon 可能不再自动下载，直接使用环境中的版本
      - name: Build APK
        run: mvn -Pandroid gluonfx:build gluonfx:package -e

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: keenotes-android-apk
          path: target/gluonfx/aarch64-android/**/*.apk
          retention-days: 5